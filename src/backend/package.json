{
  "name": "@3body/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/main.ts",
    "start": "bun src/main.ts",
    "build": "bun build --compile --target=bun-linux-x64 src/main.ts --outfile dist/3body-server"
  },
  "dependencies": {
    "@3body/shared": "*"
  },
  "devDependencies": {
    "@types/bun": "^1.1.13"
  }
}
